apiVersion: v1
kind: Service
metadata:
  name: weather-ml-app-service
  labels:
    app: weather-ml-app
spec:
  type: NodePort
  selector:
    app: weather-ml-app
  ports:
    - port: 5000
      targetPort: 5000
      nodePort: 30007
